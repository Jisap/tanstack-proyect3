import { createAuthClient } from 'better-auth/react'
export const authClient = createAuthClient({
  //baseURL: 'http://localhost:3000',
  baseURL: 'https://tanstack-proyect3-kgo3zuhmo-jisaps-projects.vercel.app'
})